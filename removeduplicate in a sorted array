def removeDuplicates(nums):
    n = len(nums)
    j = 1

    for i in range(1, n):
        print(f"i={i}, nums[i]={nums[i]}, nums[i-1]={nums[i-1]}")
        if nums[i] != nums[i - 1]:
            nums[j] = nums[i]
            
            j += 1
        


    return j
